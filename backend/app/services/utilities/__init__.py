"""
Stateless utility services (multiple instantiation).

These services are stateless utilities that can be instantiated
multiple times across different scopes (SensorManager, UnitRuntimeManager).
"""
